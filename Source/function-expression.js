/* setTimeout( function()
    {console.log('I waited at the restaurant for 2 seconds');
  },2000);
*/

  /* let counter = 0;
  function timeout(){
    setTimeout(function(){
      console.log("This is a message from the timer " + counter++);
      timeout()
    },2000)
  }

  timeout() */

  // ctrl + c to stop looping


  (function(){
    console.log('Immediately invoked function expression(IIFE)')
  })();